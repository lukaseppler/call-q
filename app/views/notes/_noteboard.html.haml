#notes
  %h2 Notes
  - if phone.notes.size > 0
    - for note in phone.notes
      %p
        %i= "#{distance_of_time_in_words_to_now(note.created_at)} ago"
        = note.content
  - else
    You haven't added a note yet!

  %br
  = form_for [phone, Note.new] do |f|
    %br
    = f.label :content
    %br
    = f.text_area :content
    %br
    = f.submit "Create note"